#include <cstdio>
#include <iostream>
#define For(i,x,y) for(int i = (x); i <= (y); i ++ )
#define fori(i,x,y) for(int i = (x); i < (y); i ++ )
using namespace std;
int main(){
    double x; cin>>x;
    int ans = x + 0.5;
    cout<<ans<<endl;
    return 0;
}