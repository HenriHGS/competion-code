#include <cstdio>
#include <cstring>
#include <vector>
#include <iostream>
using namespace std;

#define For(i,x,y) for(int i = (x); i <= (y); i ++ )
using vi = vector<int>;
const int N = 1e5+10;
int dp[N][30];
int pre[N];
int main(){
    ios::sync_with_stdio(0);
    int n;
    cin>>n;
    return 0;
}